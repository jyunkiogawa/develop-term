<%= form_for(current_user.active_relationships.find_by(followed_id: @user.id),url: relationship_path(@user.id),
             html: { method: :delete }) do |f| %>
  <%= f.submit "Unfollow", class: "btn" %>
<% end %>